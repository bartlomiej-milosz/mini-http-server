[project]
name = "tcp-server"
version = "0.1.0"
description = "TCP server implementation"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "pytest-mock>=3.15.1",
    "ruff>=0.15.12",
    "ty>=0.0.33",
]
